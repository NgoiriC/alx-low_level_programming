#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @x: The number to be checked
 * Return: Always 0
 */

int _isupper(int x)

{
	if (x >= 'A' && x <= 'Z')
	{
	return (1);
	}
	return (0);
}
